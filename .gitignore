target/
.otel-build/
*.pyc
__pycache__/
.env
.env.*
*.env.toml
!.env.example
# The Flutter app bundles its dotenv files as browser-served assets (public
# URLs/tuning only — the import script scans them for credentials).
!ee/ui/app/assets/.env
!ee/ui/app/assets/.env.*
.venv/
node_modules/
*.egg-info/
dist/
.terraform/
terraform.tfstate*
*.tfstate*
.DS_Store
uv.lock
omp.cache

# IDE
.idea/
.vscode/settings.json
*.swp
*.swo
.envrc

# Local MCP config — contains a registry credential, do not commit
.mcp.json
