server {
  listen 4001;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location = /api/v1/platform/logs {
    proxy_pass http://nasiko-backend:8000/api/v1/platform/logs;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
