version: '3.8'

services:
  business-intelligence:
    build: .
    ports:
      - "5000:5000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - .:/app
    working_dir: /app
    command: ["python", "__main__.py", "--host", "0.0.0.0", "--port", "5000"]